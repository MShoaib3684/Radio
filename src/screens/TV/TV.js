import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const TV = ({ navigation }) => {
    return (
        <View>
            <Text>TV</Text>
        </View>
    )
}

export default TV

const styles = StyleSheet.create({})
